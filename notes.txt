To encounter, 'node-waf' not found error while installing db-mysql, the following solutions have been implemeneted.

sudo npm install -g node-gyp
sudo apt-get install libcurl3-dev

alternatively, just installed
sudo npm install -g mysql
sudo npm install -g node-mysql

remove bind-address form /etc/mysql/my.cnf

we are just using mongodb as of now
apt-get install mongodb
npm install mongodb



===Issues===
DONE-1. User must not log in via two tabs or windows
DONE-2. Make no. of options 1 after setvote
DONE-3. Do superusers have uplaod privilieges?
4.Liechtenstein data on students





























===New Issues===
1. Open all downloads in new tab
2. Latest message in bold and red
3. Abstain be grey
4. Rename final version with country name
5. Lock final version after printed
6. Have 'feedback' in flags
